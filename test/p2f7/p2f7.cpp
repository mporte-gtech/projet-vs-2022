#include <iostream>

int* GetSecretWordIndex();
bool IsIndexUsed();
AddUsedIndex();
void GetPlayerGuess();
void CompareWords();
void PlayOneWord();

int main()
{
	const int wordLength = 5;
	const int maxTryAmount = 6;
	std::string availableWords[] = {"pomme", "table", "livre", "chaud", "arbre"};
	int* foundWords = (int*)malloc(sizeof(int) * (sizeof(availableWords) / sizeof(*availableWords)));

	srand(time(NULL));
	GetSecretWordIndex


}

int* GetSecretWordIndex()
{
	int isWordAvailable = 0;
	do
	{

	} while (1==0);
	//return rand() % (max - min + 1) + min;

}

bool IsIndexUsed(int index, int* table)
{
	for (size_t i = 0; i < length; i++)
	{

	}
}


